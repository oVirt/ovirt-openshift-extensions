apiVersion: v1
kind: ConfigMap
metadata:
  name: ovirt
  namespace: kube-system
data:
  connection: |
    url=https://hostname/ovirt-engine/api
    username=admin@internal
    password=pass
    insecure=false
    cafile=
